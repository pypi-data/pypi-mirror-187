from setuptools import setup, find_packages

setup(
    name='bsdb',
    version='2.0.1',
    license='MIT License',
    author='Eugênio S. Benfica',
    keywords=['database', 'json', 'csv'],
    description='A database manager based on json and csv.',
    packages=find_packages(),
    install_requires=[],
)