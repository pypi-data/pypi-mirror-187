[tool.poetry]
name = "bagitfs"
version = "0.4.0"
description = "Bagit package"
authors = ["Radim Spigel <spigel@cesnet.cz>"]

[tool.poetry.dependencies]
python = "^3.6"
bagit = "1.8.1"
fs = "^2.2.1"

[tool.poetry.dev-dependencies]
pytest = "^3.0"

[build-system]
requires = ["poetry>=0.12", "bagit", "fs"]
build-backend = "poetry.masonry.api"
