from setuptools import setup



VERSION = '0.0.1'
DESCRIPTION = 'Streaming video data via networks'

# Setting up
setup(
	name="wrapperpackage",
	version=VERSION,
    	author="NeuralNine (Florian Dedov)",
    	author_email="<mail@neuralnine.com>",
    	description=DESCRIPTION,
    	packages=['wrapperpackage'],
    	install_requires=[]
)
