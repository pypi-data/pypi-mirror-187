from setuptools import setup,find_packages
VERSION = '0.0.1'

setup(
    name="musong",
    version=VERSION,
    author="musong",
    packages=find_packages(),
    install_requires=[],
    keywords=[],
)