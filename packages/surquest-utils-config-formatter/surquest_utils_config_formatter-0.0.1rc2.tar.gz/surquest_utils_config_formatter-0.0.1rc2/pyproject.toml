[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "surquest-utils-config-formatter"
version = "0.0.1rc2"
description = "Standalone Loader class for simplification of loading configurations from YAML or JSON files."
authors = [
    {name= "Michal Švarc", email= "michal.svarc@surquest.com"}
]
readme = "README.md"
dependencies = [
]


[project.urls]
"Homepage" = "https://github.com/surquest/python-utils-loader"
"Bug Tracker" = "https://github.com/surquest/python-utils-loader/issues"

[tool.hatch.build.targets.wheel.sources]
"src" = ""
"scripts" = "surquest"

[tool.hatch.build.targets.sdist.sources]
"src" = ""
"scripts" = "surquest"
