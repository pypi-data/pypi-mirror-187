[tool.poetry]
name = "pycounts-ik"
version = "0.1.0"
description = ""
authors = ["Ivan Khokhlov <khokhlov.ivan@protonmail.com>"]
readme = "README.md"
packages = [
    { include = "src/pycounts-ik" }
]

[tool.poetry.dependencies]
python = ">=3.10.0"
matplotlib = ">=3.6.3"

[tool.poetry.dev-dependencies]

[tool.poetry.group.dev.dependencies]
pytest = ">=7.2.1"
pytest-cov = "^4.0.0"
jupyter = "^1.0.0"
myst-nb = {version = "^0.17.1", python = "^3.9"}
sphinx-autoapi = "^2.0.0"
sphinx-rtd-theme = "^1.1.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"