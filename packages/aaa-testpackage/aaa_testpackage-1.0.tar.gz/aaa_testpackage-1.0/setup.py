from setuptools import setup, find_packages

setup(
    name='aaa_testpackage',
    version='1.0',
    author='Adam mc python dev',
    packages=find_packages(),
    install_requires=['requests'],
)