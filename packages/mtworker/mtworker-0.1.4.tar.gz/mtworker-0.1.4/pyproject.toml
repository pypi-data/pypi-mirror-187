[tool.poetry]
name = "mtworker"
version = "0.1.4"
description = ""
authors = ["a <a@a.com>"]
readme = "README.md"
packages = [{include = "demo_package"},{include = "mtworker"}]
[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.7.0"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"


[project.scripts]
mtworker = "mtworker.main:cli"