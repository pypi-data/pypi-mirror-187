[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=50.0.0", "wheel"]

[project]
name = "geostream"
dynamic = [
    "authors",
    "classifiers",
    "description",
    "license",
    "readme",
    "requires-python",
    "urls",
    "version",
]